version: "3.1"

rules:

  - rule: tsq flow
    steps:
      - intent: tsq
      - action: action_tsq
      - action: action_clear_service_name_slot
      - action: action_clear_source_ip_slot
      - action: action_clear_destination_ip_slot
